using UnityEngine;

public class GameManagerSamaple : MonoBehaviour
{
    [SerializeField] int soundVolume;
    // 이런식으로 게임 전체에 대한 매니저..
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    // CardinalManager cardinalManager; <- 카디널 매니저를 멤버로 가짐.. 이런식으로 생각했습니다.

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
